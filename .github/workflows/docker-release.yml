name: "CI - Docker Image (Nightly)"

on:
    push:
        branches: [master]
    pull_request:
        branches: [master]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            - name: Build Docker Image
              run: docker build ./engine --file Dockerfile -tag alchemy:latest
